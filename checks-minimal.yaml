# Minimal Smoke Test Checks
#
# Fast validation subset for Argo Workflows post-deploy checks.
# Use -checks=/app/checks-minimal.yaml for quick validation.
#
# For full validation, use checks.yaml (14 checks across 6 layers).

checks:
  # ========================================================================
  # Layer 1: Gateway Infrastructure (quick validation)
  # ========================================================================

  - name: "Gateway Resources Programmed"
    description: "Verify all Gateway resources are programmed and have addresses"
    layer: 1
    script:
      path: "./scripts/infra/gateway-programmed.sh"
    expect:
      gating: true
# Build trigger - parallel stage test
# Trigger build 1767485388
# Build 18:12
